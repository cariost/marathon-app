# marathon-app

This project now includes a simple Angular frontend located in the `frontend` directory. The frontend uses the latest Angular version and provides a basic login page for user authentication.

## Frontend

The Angular module can be served locally with:

```bash
cd frontend
npm install
npm start
```

A login form is available at the root route.
